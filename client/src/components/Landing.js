import React from 'react';

const Landing = () => {
  return(
    <div style = {{ textAlign: 'center' }}>
      <h1>
      Survey App
      </h1>
      Collect feedback with emails
    </div>
  );
};

export default Landing;
